<!DOCTYPE html>
<html>
<head>
  <title>Kakomon</title>
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>

<body>
  <nav class="navbar navbar-default navbar-fixed-top">
    <div class="container">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
          <span class="sr-only">navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <a class="navbar-brand" href=<%= root_path %>>過去問データベース</a>
      </div>
      <div id="navbar" class="collapse navbar-collapse">
        <ul class="nav navbar-nav">
          <% if current_page?(:editor_root) %>
            <li class="active"><a>Home</a></li>
          <% else %>
            <li><%= link_to "Home", :editor_root %></li>
          <% end %>
          <% if current_page?(:search_editor_past_questions) %>
            <li class="active"><%= link_to "Search", :search_editor_past_questions %></li>
          <% else %>
            <li><%= link_to "Search", :search_editor_past_questions %></li>
          <% end %>
          <% if current_page?(:editor_tags) %>
            <li class="active"><a>Tag</a></li>
          <% else %>
            <li><%= link_to "Tag", :editor_tags %></li>
          <% end %>
        </ul>
        <ul class="nav navbar-nav navbar-right">
          <% if editor_member %>
              <li><%= link_to "編集者ページ", :editor_root %></li>
            <% if super_member %>
              <li><%= link_to "管理者ページ", :admin_root %></li>
            <% end %>
          <% end %>
          <% if current_member %>
            <li><%= link_to "ログアウト", :session, method: :delete,
              data: { confirm: "ログアウトしますか？" }  %><span class="sr-only">(current)</span></li>
          <% else %>
            <li><%= link_to "ログイン", :members %><span class="sr-only">(current)</span></li>
          <% end %>
        </ul>
      </div>
    </div>
  </nav>

    <%= yield %>

  </body>
</html>
