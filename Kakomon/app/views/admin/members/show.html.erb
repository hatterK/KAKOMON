<div class="container">
  <% @page_title = "メンバー情報" %>

  <h1><%= @page_title %></h1>
  <p><%= flash.notice %></p>

  <%= render "body" %>
  <p><%= link_to "編集", [:edit, :admin, @member] %></p>
  <p><%= link_to "削除", [:admin, @member], method: :delete,
    data: { confirm: "本当に削除しますか？" } %></p>
</div>
