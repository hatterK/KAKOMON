<h1>アップロード</h1>
<p><%= flash.notice %></p>
<%= form_for [:editor, @past_question] do |f| %>

<%= render 'form', f: f %>

<table>
  <tr>
    <th><%= f.label :ファイルアップロード %></th>
    <td><%= f.file_field :image %></td>
  </tr>
</table>
<p>
	<%= f.submit %>
</p>

<% end %>
